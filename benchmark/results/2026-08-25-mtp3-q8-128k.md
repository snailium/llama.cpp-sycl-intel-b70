# 2026-08-25 — mtp3-q8-128k (upgrade stack tagging-gate validation)

**Configuration:** `benchmark/configs/mtp3-q8-128k.md` — Q8_0 MTP draft + Q8_0 mmproj, MTP3 (n-max 3, p-min 0.1), 128k context, q8_0 KV.

**Image/stack:** `ghcr.io/snailium/llama.cpp-sycl-intel-b70/llama-sycl-b70` — `server-c26.31.39395.13-*` (compute-runtime 26.31.39395.13 / IGC 2.40.13 / level-zero 1.32.0). Validated then promoted to `:stable` / `:v0.2.1` (digest `sha256:4b7923e9…`).

This run is the **tagging-gate validation**: the upgrade stack previously crashed (`Failed to allocate physical memory`, SIGSEGV) on any per-slot context ≥ 128k with a BF16 MTP draft. Switching the draft to **Q8_0** frees ~1.5 GB VRAM and resolves the crash while keeping 128k capability.

## Environment

- GPU: single Intel Arc Pro B70 32 GB (BMG-G31, SYCL0, 32656 MiB).
- Container ports: 18080 → 8080. Env: `ONEAPI_DEVICE_SELECTOR=level_zero:0`, `SYCL_CACHE_PERSISTENT=0`, `ZES_ENABLE_SYSMAN=1`.
- Monkey-load harness: `headless_lc` / dsh agent suite (T3/T4/T5) + direct tasks (T1/T2) + vision (V1–V3).

## Results

| Test | Outcome |
|------|---------|
| **T1** direct HTML | ✅ pass (earlier candidate validation) |
| **T2** direct SVG | ✅ pass (earlier candidate validation) |
| **V1–V3** vision | ✅ pass (earlier candidate validation) |
| **T3** mqtt2ha security audit (dsh agent) | ✅ **EXIT=0**, 11.2 KB report |
| **T4** host JSON (dsh agent) | ✅ **EXIT=0**, 3.5 KB report |
| **T5** YOW snowfall research (dsh agent, longest) | ✅ **EXIT=0** — 310 cm full answer |
| Container health / stability | ✅ **51+ min healthy, 0 crashes** across the full suite |

### T5 detail (the previous 96k-context failure)

On 96k the T5 agent hit `prompt.n_tokens() = 98303, n_ctx = 98304` and stopped before finishing. On **128k** it completed:
- Station: STN_ID **49568 "OTTAWA INTL A"** (YOW, successor of old 4337, no observation gap).
- `TOTAL_SNOW` sum (LWE) = **258.6 mm**; unit-verified via `SNOW_ON_GROUND` ~10:1 ratio → **≈ 259 cm observed**.
- 2025-08-25 → 2025-12-31 climate-average window = **+51 cm** → **total ≈ 310 cm** simulated.
- Cross-checked ERA5 (145.7 cm, acknowledged discrepancy, event days agree).

## Speculative acceptance & speed (Q8 MTP, T3/T4/T5)

- **Weighted acceptance: 0.5669** (32783/57831 across 66 records); per-record mean/median 0.679/0.678; **mean len 3.04**.
- Matches the old BF16 MTP baseline (0.5670) — **no acceptance loss from Q8 draft**.
- Decode ≈ **24–28 t/s** across the long chain (≈8% above the ≈23.2 t/s no-draft baseline); short agent turns dip to ~19–21 t/s (draft overhead on short generations).

## Recommendation

Adopt `mtp3-q8-128k` as the default production config on the upgrade stack. The Q8 draft is the enabling change for full 128k on `:stable`. See the config doc for full launch flags and memory footprint.

## Artifacts

- Config: `benchmark/configs/mtp3-q8-128k.md`
- Suite logs archived on the B70 server logs archive and mirrored locally under `benchmark-cand2631/logs/`.