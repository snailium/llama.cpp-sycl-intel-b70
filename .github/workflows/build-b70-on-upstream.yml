# DEPRECATED — This single workflow has been replaced by two dedicated workflows:
#
#   - .github/workflows/build-stable.yml   (main branch, 4h + deps tracking)
#   - .github/workflows/build-dev.yml      (dev branch, weekly + special 2a/2b logic)
#
# The old combined b/v logic is no longer used.
# This file is kept only for git history. It has no active triggers.

name: (DEPRECATED) Old combined build

on:
  # All triggers removed on purpose
  workflow_dispatch:
    inputs:
      note:
        description: "This workflow is deprecated. Use build-stable or build-dev instead."
        required: false
        default: "deprecated"

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is deprecated."
          echo "Please use the separate build-stable.yml and build-dev.yml workflows."
          exit 0
